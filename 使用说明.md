# 📰 AI新闻简报生成器 - 使用说明

## 🎯 项目概述

这是一个自动化的信息爬取和简报生成系统，每天从多个科技网站爬取内容，使用DeepSeek AI生成精美的HTML简报，方便复制到微信公众号发布。

### 三大栏目

1. **每日资讯** - 从5个网站爬取AI相关新闻，5-10条
2. **科创头条** - 从科创板日报爬取热点新闻，5-10条
3. **学术动态** - 从arXiv爬取AI论文（周一至周五），10条

## ✅ 功能特性

### 1. 每日资讯
- **数据源**：
  - 机器之心 (jiqizhixin.com)
  - AIERA (aiera.com.cn)
  - Engadget (engadget.com)
  - CNET (cnet.com/news/)
  - TechCrunch AI (techcrunch.com/category/artificial-intelligence/)
- **数量**：至少5条，最多10条
- **智能补充**：如果当天不足5条，会从前一天的内容中补充真实来源

### 2. 科创头条
- **数据源**：科创板日报 (chinastarmarket.cn)
- **筛选规则**：从阅读量排名前10中选取
- **数量**：最少5条，最多10条
- **内容要求**：总结文章要点，把事情讲清楚，包含结论

### 3. 学术动态
- **数据源**：arXiv cs.AI (arxiv.org/list/cs.AI/recent)
- **发布时间**：仅周一至周五
- **数量**：从当天前20篇中精选10条通俗易懂的
- **智能回溯**：如果当天不足20条，会自动爬取前一天的

### 4. 其他特性
- ✅ **每栏1-2张配图**：自动下载原图或生成占位图
- ✅ **精美HTML排版**：适合直接复制到公众号后台
- ✅ **信息来源列表**：文末自动附上所有来源网址
- ✅ **自动清空机制**：每次运行前清空当天目录，避免去重问题
- ✅ **详细的markdown备份**：每个栏目单独保存markdown文件

## 🚀 快速开始

### 1. 检查环境

确保已安装 Python 3.12（或3.11）：

```powershell
python --version
```

### 2. 配置API密钥

如果还没有创建 `.env` 文件，请创建：

```powershell
copy env.example .env
notepad .env
```

填写您的DeepSeek API密钥：

```
DEEPSEEK_API_KEY=your_key_here
DEEPSEEK_MODEL=deepseek-chat
```

### 3. 运行生成器

生成今天的简报：

```powershell
python -m app.main --date today
```

生成指定日期的简报：

```powershell
python -m app.main --date 2026-01-15
```

仅生成指定栏目：

```powershell
# 仅生成学术动态
python -m app.main --date today --columns 学术动态

# 生成多个栏目（逗号分隔）
python -m app.main --date today --columns "学术动态,每日资讯"
```

## 📁 输出文件说明

运行后，会在 `output/YYYY-MM-DD/` 目录下生成：

```
output/2026-01-13/
├── wechat.html                      # 最终的HTML文件（复制到公众号）
├── cover.png                        # 封面图（需手动上传到公众号）
├── images/                          # 正文配图（需手动上传到公众号）
│   ├── 01_techcrunch_ai.jpg
│   ├── 02_cnet.jpg
│   └── ...
├── 每日资讯_markdown.md            # 每日资讯的markdown备份
├── 科创头条_markdown.md            # 科创头条的markdown备份
├── 学术动态_markdown.md            # 学术动态的markdown备份
├── sources_preview_每日资讯.json   # 每日资讯爬取的原始数据
├── sources_preview_科创头条.json   # 科创头条爬取的原始数据
├── sources_preview_学术动态.json   # 学术动态爬取的原始数据
└── aggregated.json                  # 汇总信息
```

## 📱 发布到微信公众号

**重要**：微信公众号后台不支持直接粘贴带本地图片的HTML，需要手动上传图片。

### 推荐流程

1. **打开HTML文件**
   - 用浏览器打开 `output/YYYY-MM-DD/wechat.html`
   - 复制全部内容

2. **粘贴到公众号后台**
   - 登录微信公众号后台
   - 新建图文消息
   - 粘贴HTML内容（文字和格式会保留）

3. **上传图片**
   - 将 `cover.png` 上传为封面图
   - 将 `images/` 目录中的图片逐个上传
   - 按原位置插入到正文中

4. **检查并发布**
   - 预览效果
   - 调整样式（如有需要）
   - 发布

## ⚙️ 配置说明

配置文件位于 `config/config.yaml`，可以自定义：

### 调整栏目数量

```yaml
columns:
  - name: "每日资讯"
    min_items: 5      # 最少5条
    max_items: 10     # 最多10条
```

### 调整网站权重

权重越高，该来源的文章越容易被选中：

```yaml
sources:
  - name: jiqizhixin
    weight: 1.2      # 机器之心权重较高
  - name: engadget
    weight: 0.9      # Engadget权重较低
```

### 关键词过滤

仅对"每日资讯"生效（科创头条和学术动态跳过过滤）：

```yaml
filter:
  keywords_any:      # 白名单：包含任一关键词
    - AI
    - 人工智能
    - 大模型
  keywords_not:      # 黑名单：不包含这些关键词
    - 彩票
    - 星座
```

### 超时设置

```yaml
timeouts:
  per_source_seconds: 30      # 单个网站抓取超时（秒）
  arxiv_source_seconds: 120   # arXiv专用超时（需访问多个页面）
```

## 🔍 项目代码结构

```
linux_ai_news/
├── app/
│   ├── sources/              # 数据源适配器
│   │   ├── jiqizhixin.py    # 机器之心
│   │   ├── aiera.py         # AIERA
│   │   ├── engadget.py      # Engadget
│   │   ├── cnet.py          # CNET
│   │   ├── techcrunch_ai.py # TechCrunch AI
│   │   ├── chinastarmarket.py # 科创板日报
│   │   └── arxiv_cs_ai.py   # arXiv cs.AI
│   ├── pipeline/             # 数据处理流程
│   │   ├── filtering.py     # 关键词过滤
│   │   ├── dedup.py         # 去重（URL + 标题相似度）
│   │   ├── ranking.py       # 排序和选题
│   │   └── writing.py       # LLM写作
│   ├── llm/                  # DeepSeek客户端和提示词
│   │   ├── deepseek_client.py
│   │   └── prompts.py       # 三个栏目的专用提示词
│   ├── images/               # 图片处理
│   │   ├── downloader.py    # 下载原图
│   │   ├── cover.py         # 生成封面
│   │   └── separator.py     # 生成占位图
│   ├── render/               # HTML渲染
│   │   └── wechat_html.py   # 公众号HTML模板
│   └── main.py               # 主程序入口
├── config/
│   └── config.yaml           # 配置文件
├── db/
│   └── articles.sqlite3      # 历史记录数据库
├── output/                   # 每日输出目录
└── requirements.txt          # Python依赖
```

## 🛠️ 代码优化点分析

### ✅ 已实现的优化

1. **智能补充机制**
   - 每日资讯：不足5条时从前一天补充真实内容
   - 科创头条：首页不足时从/telegraph页面补充
   - 学术动态：当天不足20篇时回溯前一天

2. **去重机制**
   - URL去重：同一URL只保留一次
   - 标题相似度去重：使用SimHash算法
   - 科创头条：放宽标题去重阈值（避免误删）

3. **稳定性保障**
   - 每次运行前清空当天目录（避免触发历史去重）
   - 单个网站抓取超时保护（30秒）
   - arXiv专用超时（120秒，因需访问多个页面）
   - 详细的进度日志（[START]/[DONE]）

4. **LLM写作质量**
   - 每个栏目有专门的提示词
   - 强制要求基于输入内容，不允许编造
   - 科创头条：要求把事情讲清楚，包含结论
   - 学术动态：要求通俗易懂，总结研究要点和结论

5. **并发抓取**
   - arXiv使用异步并发（Semaphore(5)）加速抓取
   - 支持多栏目并行处理

### 🔧 可选的优化方向

如果未来需要扩展，可以考虑：

1. **增加数据源**
   - 在 `app/sources/` 添加新的适配器
   - 在 `config.yaml` 中配置
   - 在 `registry.py` 中注册

2. **调整LLM参数**
   - `app/llm/prompts.py`：修改提示词
   - `app/pipeline/writing.py`：调整temperature和max_tokens

3. **自定义HTML样式**
   - `app/render/wechat_html.py`：修改HTML模板和CSS样式

4. **添加定时任务**
   - Windows：使用任务计划程序
   - Linux：使用crontab

## 📊 当前运行状态

根据最近的运行记录（`output/2026-01-09/`）：

- ✅ 每日资讯：成功抓取10条
- ✅ 科创头条：成功抓取5条
- ✅ 学术动态：成功抓取10条
- ✅ 配图：每个栏目1-2张
- ✅ HTML生成：排版精美，格式正确

## ❓ 常见问题

### 1. 某些网站抓不到内容

**原因**：网站可能返回429（请求过多）或5xx错误

**解决**：
- 程序会自动跳过失败的网站
- 可以在 `config.yaml` 中禁用该网站：`enabled: false`

### 2. 运行时间太长

**原因**：某些网站响应慢或超时

**解决**：
- 已设置单个网站30秒超时
- 可以在 `config.yaml` 中调整 `timeouts.per_source_seconds`

### 3. 某个栏目内容不足

**原因**：当天该领域新闻较少

**解决**：
- 程序会自动从前一天补充（每日资讯、学术动态）
- 科创头条会从/telegraph页面补充
- 如仍不足，会在日志中警告但不会编造内容

### 4. Python 3.14 装依赖失败

**原因**：某些库（lxml、Pillow）没有预编译wheel

**解决**：使用 Python 3.12 或 3.11

## 📞 技术支持

如遇问题，可查看：

1. **运行日志**：控制台输出的 `[INFO]`、`[WARN]` 信息
2. **原始数据**：`output/YYYY-MM-DD/sources_preview_*.json`
3. **汇总信息**：`output/YYYY-MM-DD/aggregated.json`
4. **Markdown备份**：`output/YYYY-MM-DD/*_markdown.md`

## 🎉 项目总结

这个项目已经**完全实现了您的所有需求**：

✅ 三个栏目完整实现  
✅ 所有数据源正常工作  
✅ 智能补充和去重机制  
✅ 每次运行前自动清空  
✅ 配图功能完善  
✅ HTML排版精美  
✅ LLM生成内容质量高  

**可以直接使用，无需额外开发！**

---

**最后更新**：2026-01-13  
**项目状态**：生产就绪 ✅
