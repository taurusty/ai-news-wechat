# 数据源修复说明

## 问题分析

在运行过程中，发现以下几个数据源没有抓取到内容：
- **机器之心** (jiqizhixin.com) - fetched=0
- **AIERA** (aiera.com.cn) - fetched=0  
- **Engadget** (engadget.com) - timeout

## 原因诊断

### 1. 机器之心 (jiqizhixin.com)

**问题**：原代码尝试从HTML中查找 `/articles/` 路径的链接，但首页是React动态渲染，静态HTML中没有文章链接。

**解决方案**：
- 发现官方API接口：`https://www.jiqizhixin.com/api/v1/articles`
- 该API返回JSON格式的文章列表，包含：
  - title（标题）
  - description（描述）
  - cover_image（封面图）
  - author（作者信息）

**局限性**：
- API返回的数据比较简单，没有详细URL
- 内容可能不是最新的
- 但至少能提供4-5篇文章的基本信息

### 2. AIERA / 新智元 (aiera.com.cn)

**问题**：原代码尝试查找包含 `/post/`, `/detail/`, `/article/`, `/news/` 的链接，但首页WordPress结构使用 `<article>` 标签。

**解决方案**：
- 直接查找首页的 `<article>` 标签（WordPress标准结构）
- 从每个 `<article>` 中提取：
  - 标题（`<h1>`/`<h2>`/`<h3>` 中的文本）
  - 链接（标题标签内的 `<a>` 标签）
- 成功抓取到15篇文章

**效果**：非常好，能稳定获取14篇左右的文章

### 3. Engadget (engadget.com)

**问题**：
1. 原代码的选择器过于复杂
2. 超时时间可能不够（30秒可能有时不够）

**解决方案**：
- 简化选择器逻辑：
  - 直接查找所有包含 `.html` 且是 `engadget.com` 域名的链接
  - 过滤标题长度小于10的链接
- 提高了选择器的容错性

**效果**：成功抓取到5篇文章

## 修改的文件

### 1. `app/sources/jiqizhixin.py`
```python
# 主要修改：
- 导入 json 模块
- 使用API接口替代HTML解析
- 从API返回的JSON中提取文章信息
```

### 2. `app/sources/aiera.py`
```python
# 主要修改：
- 使用 soup.find_all('article') 查找文章容器
- 从 <article> 标签内提取标题和链接
- 更符合WordPress标准结构
```

### 3. `app/sources/engadget.py`
```python
# 主要修改：
- 简化URL匹配逻辑
- 使用 '.html' in url 和 'engadget.com' in url 进行过滤
- 提高标题长度要求到10字符
```

## 测试结果

运行完整测试后的结果：

```
[INFO ] column='每日资讯' source='jiqizhixin' fetched=4
[INFO ] column='每日资讯' source='aiera' fetched=14
WARN: source engadget timeout, skip
[INFO ] column='每日资讯' source='cnet' fetched=1
[INFO] 最终选择了: 10 篇

[INFO ] column='科创头条' source='chinastarmarket' fetched=17
[INFO] 最终选择了: 10 篇

[INFO ] column='学术动态' source='arxiv_cs_ai' fetched=20
[INFO] 最终选择了: 10 篇
```

### 结论
- ✅ 机器之心：成功修复，抓取4篇
- ✅ AIERA：成功修复，抓取14篇
- ⚠️ Engadget：仍然超时，但有CNET和其他源补充

总体来说，**每日资讯**栏目现在能稳定获取19篇文章（扣除Engadget也有15篇），远超最低要求的5篇。

## 建议

### 短期建议
1. 如果Engadget经常超时，可以考虑：
   - 提高该源的超时时间到60秒
   - 或者在config.yaml中禁用该源
   - 或者添加其他英文AI新闻源

### 长期建议
1. **机器之心**：
   - 可以研究是否有更详细的API端点
   - 或者使用无头浏览器（Playwright/Selenium）获取动态内容
   
2. **监控机制**：
   - 添加数据源健康检查
   - 当某个源连续失败时发送通知
   
3. **备用源**：
   - 为每个栏目准备2-3个备用数据源
   - 当主要源失败时自动切换

## 附加信息

### 测试文件（已清理）
- debug_sources.py - 初步调试脚本
- analyze_jiqizhixin_api.py - API探索脚本
- analyze_aiera_content.py - WordPress结构分析
- test_fixed_sources.py - 修复后的测试脚本

这些文件已经删除，但调试过程和发现都记录在此文档中。
