# 快速开始指南

## 📋 前置准备

### 1. 必需工具
- ✅ Python 3.11 或 3.12
- ✅ Git（用于从GitHub克隆）
- ✅ DeepSeek API密钥

### 2. 申请DeepSeek API密钥

1. 访问：https://www.deepseek.com/
2. 注册/登录账号
3. 进入"API密钥"页面
4. 创建新密钥并保存（后面会用到）

---

## 🚀 三步启动

### 步骤 1：安装依赖

双击运行 `运行.bat`，选择选项 `1` 进行安装：

```
请选择操作：
  1. 首次安装（创建虚拟环境并安装依赖）
  2. 运行程序（生成今日简报）
  3. 运行程序（指定日期）
  4. 查看配置文件
  5. 清理输出目录
  0. 退出
```

或手动执行：

```powershell
# 创建虚拟环境
py -3.12 -m venv .venv

# 激活虚拟环境
.\.venv\Scripts\Activate.ps1

# 安装依赖
pip install -r requirements.txt
```

### 步骤 2：配置API密钥

1. 复制 `env.example` 为 `.env`：
```powershell
copy env.example .env
```

2. 用记事本打开 `.env`：
```powershell
notepad .env
```

3. 填入你的DeepSeek API密钥：
```env
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
DEEPSEEK_MODEL=deepseek-chat
```

4. 保存并关闭

### 步骤 3：运行程序

双击 `运行.bat`，选择选项 `2`，或者：

```powershell
# 激活虚拟环境（如果还没激活）
.\.venv\Scripts\Activate.ps1

# 运行程序
python -m app.main
```

---

## 📁 查看输出

程序运行成功后，在 `output/2026-01-13/` 目录下会生成：

```
📂 output/2026-01-13/
   ├── 📄 wechat.html              ← 用浏览器打开，复制到公众号
   ├── 📁 images/                  ← 文章配图，需上传到公众号
   │   ├── 每日资讯_01_xxx.jpg
   │   ├── 科创头条_02_xxx.png
   │   └── ...
   ├── 📄 每日资讯_markdown.md
   ├── 📄 科创头条_markdown.md
   └── 📄 学术动态_markdown.md
```

---

## 📱 发布到微信公众号

### 方法 1：复制粘贴（推荐）

1. 用浏览器打开 `wechat.html`
2. 按 `Ctrl+A` 全选，`Ctrl+C` 复制
3. 登录微信公众号后台，新建图文
4. 在编辑器中 `Ctrl+V` 粘贴
5. 手动上传 `images/` 文件夹中的图片
6. 根据文件名将图片插入对应位置
7. 预览、发布

### 方法 2：分段复制

如果一次性粘贴失败，可以分三段复制：
1. 每日资讯部分
2. 科创头条部分
3. 学术动态部分

---

## ⚙️ 自定义配置（可选）

### 修改数据源

编辑 `config/config.yaml`：

```yaml
columns:
  每日资讯:
    sources:
      - jiqizhixin      # 可以注释掉不想要的源
      - aiera
      # - engadget     # 用#注释掉即可禁用
    min_items: 5        # 可以修改最少条数
    max_items: 10       # 可以修改最多条数
```

### 修改关键词过滤

```yaml
filter:
  keywords_any:         # 白名单：包含这些词的保留
    - AI
    - 人工智能
    - ChatGPT
    # 添加你关注的关键词
  
  keywords_not:         # 黑名单：包含这些词的过滤掉
    - 游戏
    - 娱乐
    # 添加你不想要的关键词
```

### 修改超时时间

如果网络较慢，可以增加超时时间：

```yaml
timeouts:
  per_source_seconds: 60      # 单个源超时（默认30秒）
  arxiv_source_seconds: 180   # arXiv超时（默认120秒）
```

---

## 🔍 常见问题

### Q1: 运行后没有生成HTML文件

**原因**：可能某个步骤出错了

**解决**：
1. 查看控制台输出，找到错误信息
2. 检查 `.env` 文件中的API密钥是否正确
3. 确认网络连接正常

### Q2: 某些栏目内容很少或为空

**原因**：数据源抓取失败或超时

**解决**：
1. 这是正常现象，程序会自动跳过失败的源
2. 可以重新运行一次
3. 或者在配置文件中增加超时时间

### Q3: 图片在公众号无法显示

**说明**：这是微信的限制，不是程序问题

**解决**：必须手动上传图片到公众号后台

### Q4: 文献链接无法点击

**检查**：
1. 确保使用的是最新版本（v1.1.0+）
2. 复制到公众号后，点击链接测试
3. 如果还是不行，可能是公众号权限问题

---

## 📅 定时运行（进阶）

### Windows任务计划程序

1. 打开"任务计划程序"
2. 创建基本任务
3. 设置触发器：每天早上8点
4. 操作：启动程序
   - 程序：`C:\Users\zang\Desktop\linux_ai_news\运行.bat`
   - 参数：留空
   - 起始于：`C:\Users\zang\Desktop\linux_ai_news`
5. 完成

现在程序会每天自动运行！

---

## 🆘 获取帮助

遇到问题？

1. 📖 查看详细文档：`README.md`
2. 🔧 查看推送指南：`推送到GitHub.md`
3. 📝 查看更新日志：`CHANGELOG.md`
4. 🐛 提交Issue：GitHub仓库的Issues页面

---

## 🎉 开始使用吧！

现在你已经准备就绪，双击 `运行.bat` 开始生成你的第一份AI资讯简报吧！
